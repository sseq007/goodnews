import ShelterBox from "./ShelterBox";

// 대피소 박스를 묶는 리스트 컴포넌트
const ShelterBoxList = () => {
  return (
    <div>
      대피소 박스를 묶는 리스트 컴포넌트
      <ShelterBox />
    </div>
  );
};

export default ShelterBoxList;
