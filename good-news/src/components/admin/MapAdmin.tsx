import styled from "styled-components";

// 관리 페이지에서 쓰이는 지도
const MapAdmin = () => {
  return <StyledMapWrapper>지도 들어가용</StyledMapWrapper>;
};

export default MapAdmin;

const StyledMapWrapper = styled.div``;
